// Authored by Karter Whitman using Claude Sonnet 5
// query/'s public surface: the shared matching primitive `sets/` and
// `search/` both consume, and neither depends on the other for.
export { matches, type MatchContext } from "./evaluate.js";
export { buildContext } from "./context.js";
