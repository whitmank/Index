// Authored by Karter Whitman using Claude Opus 4.8
// An item with no resource has no content to draw. The layout still
// renders — the item is its name, its fields and its connections, which
// is a perfectly good thing for an item to be.
export function BareRenderer() {
  return null;
}
