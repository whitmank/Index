// Author: Claude Sonnet 4.6
// Minimal host app — exists only to host the FinderSyncExtension.appex.
// Runs as a background agent (LSUIElement = true), no UI.

import Cocoa

let app = NSApplication.shared
app.run()
